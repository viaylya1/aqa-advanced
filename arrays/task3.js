// Counting the sum of all elements in the array

const numbers = [10, 20, 30, 40, 50];
const sumNumbers = numbers.reduce((acc, element) => acc + element);
console.log(`Sum of all elements in the "numbers" array: ${sumNumbers}`);
