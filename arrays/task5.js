// Combining arrays with .concat

const firstArray = ['bannana', 'lemon', undefined];
const secondArray = [['pear', 'apple'], 'apricot'];
const combinedArray = firstArray.concat(secondArray);
console.log(combinedArray);
